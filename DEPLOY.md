# رفع النظام على الاستضافة

آخر تحديث: 23/09/2026

---

## 1. الملفات اللي **متترفعش**

| الملف / الفولدر | السبب |
|---|---|
| `data/` | فيه ملف enrolment الجامعة كله (74 ألف سجل) وأسماء وإيميلات الطلاب وكلمات سر المعيدين. مالوش أي لزمة على السيرفر. |
| `config.php` | ده إعداد الجهاز المحلي — `debug = true` وقاعدة بيانات `127.0.0.1`. هتعمل واحد جديد على السيرفر. |
| `db/seed.php` | بيمسح كل الجداول ويملاها بيانات وهمية. وجوده على سيرفر فيه درجات حقيقية خطر. |
| `db/backups/` | نسخ محلية، ملهاش لازمة. |
| `db/php-errors.log` | لوج محلي. |

الباقي كله يترفع زي ما هو.

---

## 2. قاعدة البيانات

اعمل قاعدة بيانات جديدة من لوحة الاستضافة، وبعدين ارفع الـ dump:

```bash
mysql -u USER -p DBNAME < marking-20260923.sql
```

أو من phpMyAdmin: **Import** → اختار الملف.

الملف فيه كل حاجة جاهزة:

| | |
|---|---|
| حسابات | 21 (مشرفين اتنين + 19 معيد) |
| شعب | 3 (B2E · B1E · B5C) |
| طلاب | 57 |
| ربط معيدين بشعب | 16 |
| معامل | 12 بروبريك من 10 |
| جدول المعامل | لاب 1 يبدأ 20/09/2026 |

> **مهم:** الـ dump ده فيه الـ schema الكامل بما فيه جدول `audit_log` وعمود `users.lang`.
> يعني **متشغّلش** `schema.sql` ولا `migrate-*.sql` بعده — هيمسحوا الداتا.

---

## 3. الإعدادات

انسخ `config.production.php` جنب `index.html` وسمّيه `config.php`، واملا اللي عليه `TODO`:

- بيانات قاعدة البيانات
- `siteUrl` — العنوان الكامل للموقع (بيتحط في لينكات الإيميل)
- بيانات SMTP لو هتفعّل التنبيهات

سيب `'debug' => false` زي ما هو. ولو الموقع على https سيب `'secure_cookies' => true`.

---

## 4. النظام جوه سابفولدر — `/courses/cse014/`

النظام كله بيستخدم **مسارات نسبية**، فبيشتغل في أي فولدر من غير ما تعدّل حاجة.
بس في حاجتين كانوا محتاجين ضبط، واتظبطوا:

**كوكي الجلسة بقت محصورة في الفولدر.** كانت `path=/` يعني بتتبعت لكل الدومين —
الموقع الأب وأي تطبيق تاني جنبه. دلوقتي بتتحسب أوتوماتيك:

```
/var/www/html/courses/cse014  →  path=/courses/cse014/
```

نفس الكلام على كوكي اللغة. لو السيرفر ورا reverse proxy بيغيّر المسار
والاكتشاف طلع غلط، اكتبه بإيدك في `config.php`:

```php
'base_path' => 'courses/cse014',
```

**فصل الـ rewrite عن الموقع الأب.** لو `/var/www/html` فيه موقع بيوجّه كل
الطلبات لـ front controller (زي ووردبريس)، قواعده كانت هتبلع الطلبات اللي
جاية للفولدر ده. `.htaccess` بتاعنا بقى فيه `RewriteEngine On` +
`RewriteOptions IgnoreInherit` عشان يفصل نفسه.

> لو ظهرلك **500** بعد الرفع، ده على الأغلب Apache أقدم من 2.4.8 مش فاهم
> `RewriteOptions IgnoreInherit` — امسح السطر ده من `.htaccess` وجرّب تاني.

كمان هيدرات الأمان بقت `unset` قبل الـ `set`، عشان لو الموقع الأب حاطط
CSP مختلف مايتجمعش مع بتاعنا (المتصفح بيعمل intersection للاتنين وده بيكسّر
الصفحات).

اتأكدت من ده فعلياً: شغّلت النظام على `/courses/cse014/` وطلعت الكوكي
`path=/courses/cse014/`، والصفحات اشتغلت بالعربي والإنجليزي عادي.

---

## 5. Apache ولا nginx؟

**Apache** — ملفات `.htaccess` موجودة وبتشتغل لوحدها:
منع `config.php` ومجلد `db/` ومجلد `data/`، وهيدرات الأمان (CSP و `X-Frame-Options` و `nosniff`).

بعد ما تتأكد إن الموقع كله https، شيل التعليق عن السطر ده في `.htaccess`:

```apache
Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
```

**nginx** — ملفات `.htaccess` **مش هتشتغل خالص**. لازم تحط ده في الـ server block:

```nginx
location ~ ^/(config\.php|config\.sample\.php) { deny all; }
location ~ ^/(db|data)/                        { deny all; }
location ~ \.(sql|log|md)$                     { deny all; }

add_header X-Content-Type-Options "nosniff"  always;
add_header X-Frame-Options        "DENY"     always;
add_header Referrer-Policy        "same-origin" always;
```

> الـ API بيبعت نفس هيدرات الأمان من `api/_bootstrap.php`، فالـ endpoints محمية في الحالتين.
> الناقص على nginx هو حماية الصفحات الساكنة ومنع الملفات.

---

## 6. المهام الدورية

ضيف في cron بتاع الاستضافة:

```
*/5 * * * * php /var/www/html/courses/cse014/tools/cron.php >/dev/null 2>&1
```

بتعمل حاجتين: تبعت طابور الإيميلات، وتاخد نسخة احتياطية كل 24 ساعة في `db/backups/`.

لو الاستضافة مفيهاش CLI cron، حط `token` في `config.php` وناد العنوان ده من أي خدمة cron خارجية:

```
https://YOUR-HOST/courses/cse014/tools/cron.php?token=...
```

---

## 7. بعد الرفع — اتأكد من ده

- [ ] افتح الموقع، سجّل دخول بـ `elridiny`
- [ ] اللوحة بتفتح وبتشوف الـ 3 شعب والـ 57 طالب
- [ ] **من المتابعة → من المسؤول عن من** — التوزيع ظاهر صح
- [ ] جرّب زرار اللغة — الواجهة بتقلب عربي/إنجليزي
- [ ] افتح `https://YOUR-HOST/courses/cse014/config.php` — لازم يرجّع **403**
- [ ] الموقع الأب لسه شغال عادي (النظام ده مبيأثرش عليه)
- [ ] افتح `https://YOUR-HOST/courses/cse014/db/schema.sql` — لازم يرجّع **403**
- [ ] غيّر كلمة سر `elridiny` من **المستخدمين**
- [ ] قرر تعمل إيه بحساب `main` المؤقت (شوف تحت)

---

## 8. حسابات المشرفين

| الحساب | الاسم | ملاحظة |
|---|---|---|
| `elridiny` | Elridiny | المشرف العام الفعلي |
| `main` | م. عبدالله | حساب مؤقت عملته وقت التركيب |

النظام **بيمنع** إيقاف آخر مشرف عام نشط، فلو عايز تشيل `main` لازم `elridiny` يكون شغال الأول.

أنسب حاجة: سيب `main` شغال كحساب احتياطي بس **غيّر كلمة سره**، عشان لو كلمة سر `elridiny` ضاعت مايبقاش في قفل على اللوحة.

---

## 9. الدرجات

`marks` فاضي — مفيش ولا درجة مسجلة لسه. المعيدين يقدروا يبدأوا لاب 1 على طول بعد الرفع.

لاب 1 مفتوح من 20/09 لـ 27/09، ولاب 2 هيفتح لوحده بعدها من غير أي تدخل.
